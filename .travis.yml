language: php
php:
  - '7.2'

dist: trusty

services:
  - docker



#addons:
  #sonarcloud:
    #organization: "tenecort"
    #token:
      #secure: $SONAR_TOKEN # encrypted value of your token

git:
  depth: false
  
script:
   - wget https://phar.phpunit.de/phpunit-8.3.phar
   - chmod +x phpunit-8.3.phar
   - ./phpunit-8.3.phar --bootstrap tests/init.php tests/.
   - sudo apt-get update
   #- sudo apt-get install -o Dpkg::Options::="--force-confold" --force-yes -y docker-engine
   - docker-compose --version
   - docker-compose up -d
   #- ./phpunit-8.3.phar --coverage-clover=coverage-report.clover --log-junit=test-report.xml
   #- sonar-scanner
